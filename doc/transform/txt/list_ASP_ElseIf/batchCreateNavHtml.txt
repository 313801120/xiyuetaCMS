'批量生成导航到html
elseif request("act")="batchCreateNavHtml" then
    if checkAdminRule("网站栏目生成html")=false then
        call addSystemLog(tableName,"批量生成导航html失败，没有【网站栏目生成html】权限")  
        call die("{""info"": ""批量生成导航html失败，没有【网站栏目生成html】权限"",""status"": ""n""}")
    end if
    call echoB("批量生成导航html完成","共 "&batchCreateNavHtml(id,0) & " 条")
    call die(getTimer())